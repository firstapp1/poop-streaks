platform :ios, '18.2'

target 'poop-streaks' do
  use_frameworks!

  # Firebase dependencies
  pod 'Firebase/Auth'
  pod 'Firebase/Firestore'
  pod 'Firebase/Messaging'

  # Use the gRPC Pod
  pod 'gRPC'
end